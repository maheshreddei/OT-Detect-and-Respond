# 03 — Protocol References

Per-protocol attack-surface references backing the protocol-level detections in
`02-detection/`. Each covers the protocol's structure, the functions that matter for
security, what normal looks like, and what to detect.

Modbus · DNP3 · IEC 60870-5-104 · IEC 61850 · S7comm · EtherNet/IP–CIP · OPC UA · BACnet ·
MQTT · FTP · HTTP · VNC · Nmap/scanning behaviour

Companion: `../docs/protocol-attack-surface-model.md` for the cross-protocol model.
