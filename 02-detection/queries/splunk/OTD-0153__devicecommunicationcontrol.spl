``` OTD-0153 - DeviceCommunicationControl ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: BAC-03 ```
``` ATT&CK for ICS: T0814  |  Severity: high ```
``` Detection logic: Any DeviceCommunicationControl service ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- Any DeviceCommunicationControl service ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
