``` OTD-0168 - Post-login control action ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: HTTP-04 ```
``` ATT&CK for ICS: T0831  |  Severity: high ```
``` Detection logic: Config/upload/start-stop endpoints hit after login ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- Config/upload/start-stop endpoints hit after login ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
