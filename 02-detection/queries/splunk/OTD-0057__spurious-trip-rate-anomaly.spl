``` OTD-0057 - Spurious trip-rate anomaly ```
``` Library: SIS Safety Detection  |  Legacy ID: SIS-F3 ```
``` ATT&CK for ICS: T0880  |  Severity: medium ```
``` Detection logic: fail-safe ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_historian earliest=-24h
``` TODO: express the detection condition -- fail-safe ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
