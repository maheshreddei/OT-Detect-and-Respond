``` OTD-0182 - Write from non-engineering source ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: S7-04 ```
``` ATT&CK for ICS: T0836  |  Severity: high ```
``` Detection logic: Write-var from host outside EWS baseline ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network sourcetype=zeek:s7comm earliest=-24h
``` TODO: express the detection condition -- Write-var from host outside EWS baseline ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
