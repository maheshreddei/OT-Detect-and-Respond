``` OTD-0179 - Browse sweep (recon) ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: OPC-04 ```
``` ATT&CK for ICS: T0846  |  Severity: medium ```
``` Detection logic: Broad Browse/Read across address space from new client ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network sourcetype=zeek:opcua earliest=-24h
``` TODO: express the detection condition -- Broad Browse/Read across address space from new client ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
