``` OTD-0174 - Anonymous/new client connect ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: MQTT-03 ```
``` ATT&CK for ICS: T0822  |  Severity: medium ```
``` Detection logic: CONNECT with no auth or new client id ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- CONNECT with no auth or new client id ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
