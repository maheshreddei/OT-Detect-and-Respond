``` OTD-0164 - Firmware/project file transfer ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: FTP-04 ```
``` ATT&CK for ICS: T0873  |  Severity: high ```
``` Detection logic: Firmware/project extensions transferred to/from OT ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- Firmware/project extensions transferred to/from OT ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
