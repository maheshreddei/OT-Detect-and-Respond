``` OTD-0032 - SIS traffic bypassing sanctioned gateway ```
``` Library: SIS Safety Detection  |  Legacy ID: SIS-A5 ```
``` ATT&CK for ICS: T0855  |  Severity: high ```
``` Detection logic: independence ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=firewall earliest=-24h
``` TODO: express the detection condition -- independence ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
