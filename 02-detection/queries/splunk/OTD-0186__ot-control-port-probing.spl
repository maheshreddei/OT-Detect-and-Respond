``` OTD-0186 - OT control-port probing ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: SCAN-04 ```
``` ATT&CK for ICS: T0846  |  Severity: high ```
``` Detection logic: Probes to 502/102/2404/20000/44818/47808 from non-master ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- Probes to 502/102/2404/20000/44818/47808 from non-master ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
