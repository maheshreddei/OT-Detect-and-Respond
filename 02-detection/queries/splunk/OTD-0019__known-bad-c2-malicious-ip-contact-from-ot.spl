``` OTD-0019 - Known-bad C2/malicious IP contact from OT ```
``` Library: OT Threat Content  |  Legacy ID: OT-UC-0019 ```
``` ATT&CK for ICS: T0885  |  Severity: High ```
``` Detection logic: C2 ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_nozomi sourcetype=nozomi:alert earliest=-24h
``` TODO: express the detection condition -- C2 ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
