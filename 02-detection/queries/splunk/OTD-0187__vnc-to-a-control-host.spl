``` OTD-0187 - VNC to a control host ```
``` Library: OT Protocol Defense (NDR)  |  Legacy ID: VNC-01 ```
``` ATT&CK for ICS: T0822  |  Severity: high ```
``` Detection logic: Session to EWS/HMI/PLC from any source ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_network earliest=-24h
``` TODO: express the detection condition -- Session to EWS/HMI/PLC from any source ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
