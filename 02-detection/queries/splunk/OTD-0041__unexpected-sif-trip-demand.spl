``` OTD-0041 - Unexpected SIF trip/demand ```
``` Library: SIS Safety Detection  |  Legacy ID: SIS-C3 ```
``` ATT&CK for ICS: T0880  |  Severity: high ```
``` Detection logic: trip-integrity ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_historian earliest=-24h
``` TODO: express the detection condition -- trip-integrity ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
