``` OTD-0016 - Alarm suppression/modify alarm settings ```
``` Library: OT Threat Content  |  Legacy ID: OT-UC-0016 ```
``` ATT&CK for ICS: T0878  |  Severity: High ```
``` Detection logic: Evasion ```
``` STATUS: SCAFFOLD - complete and validate before deployment. ```
index=ot_historian earliest=-24h
``` TODO: express the detection condition -- Evasion ```
| stats count AS event_count, min(_time) AS first_seen, max(_time) AS last_seen
    BY src_ip dest_ip
| where event_count > <THRESHOLD>
| convert ctime(first_seen) ctime(last_seen)
| sort - event_count
