// OTD-0149 - GOOSE stNum/sqNum anomaly
// Library: OT Protocol Defense (NDR)  |  Legacy ID: 61850-03
// ATT&CK for ICS: T0856  |  Severity: critical
// Detection logic: Out-of-sequence or jumped state number (spoof signature)
//
// STATUS: SCAFFOLD. This detection is catalogued as a specification; the logic below
// expresses the intent against the mapped table but MUST be completed and validated
// against real telemetry before deployment. Placeholders in <angle brackets> are
// environment-specific.
//
OTNetwork_CL
| where TimeGenerated > ago(24h)
// TODO: express the detection condition -- Out-of-sequence or jumped state number (spoof signature)
| where isnotempty(SourceIP)
| summarize Count = count(), FirstSeen = min(TimeGenerated), LastSeen = max(TimeGenerated)
    by SourceIP, DestinationIP
| where Count > <THRESHOLD>
| order by Count desc
