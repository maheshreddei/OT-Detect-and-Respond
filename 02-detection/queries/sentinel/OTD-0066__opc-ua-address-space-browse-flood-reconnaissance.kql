// OTD-0066 - OPC UA Address Space Browse Flood (Reconnaissance)
// Source: 02-detection/sigma/ot-ics-soc/12_opc-ua-address-space-browse-flood-reconnaissance.yml
// ATT&CK for ICS: attack.ics.t0846, attack.ics.t0888
// Severity: medium
// PLACEHOLDERS in <angle brackets> must be baselined before use.
//
OTNetwork_CL
| where (Protocol == "opcua" and service == "Browse")
| summarize Count = count(), FirstSeen = min(TimeGenerated), LastSeen = max(TimeGenerated) by SourceIP, DestinationIP // original Sigma timeframe: 5m
| project TimeGenerated, SourceIP, DestinationIP, Protocol, FunctionCode, AccountName, Computer
| order by TimeGenerated desc
